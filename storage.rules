rules_version = '2';

// L'Atelier n'utilise pas Cloud Storage (avatars et couvertures sont
// compressés et stockés dans Firestore). Tout accès est refusé.
service firebase.storage {
  match /b/{bucket}/o {
    match /{allPaths=**} {
      allow read, write: if false;
    }
  }
}
